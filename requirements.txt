# Core FastAPI dependencies
fastapi==0.110.0
uvicorn[standard]==0.35.0
jinja2==3.1.2
python-multipart==0.0.9

# Database dependencies (SQLite compatible)
sqlalchemy==2.0.23

# File handling and processing (WebContainer compatible)
aiofiles==23.2.1

# HTTP and networking
requests==2.31.0
urllib3==2.2.1

# Utilities
python-dotenv==1.0.1
pytz==2024.1

# Authentication dependencies (removed bcrypt - not compatible with WebContainer)
# passlib[bcrypt]==1.7.4
# bcrypt==3.2.2

# Database migrations (removed - requires additional system dependencies)
# alembic==1.13.1

# Image processing (removed - requires system libraries)
# pillow==11.0.0

# Data processing (removed - requires C extensions)
# pandas==2.2.2; python_version < "3.13"
# pandas==2.2.3; python_version >= "3.13"

# Excel processing (removed - requires C extensions)
# openpyxl==3.1.2

# HTML to image conversion (removed - requires external binaries)
# html2image==2.0.5